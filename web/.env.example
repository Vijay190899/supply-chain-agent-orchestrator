# Point the UI at the orchestrator backend for live runs.
# Leave unset (or omit this file) to run in self-contained browser demo mode.
#
# Local backend:   NEXT_PUBLIC_API_URL=http://localhost:8080
# Deployed backend: NEXT_PUBLIC_API_URL=https://<your-cloud-run-url>
NEXT_PUBLIC_API_URL=
